# MySQL 连接池耗尽

## 典型现象
- 日志出现 `Too many connections`、`pool timeout` 或请求长时间等待数据库连接。
- 数据库本身可能正常，但应用连接没有及时归还。

## 排查步骤
- 查看活跃连接、等待连接、连接池上限和慢 SQL 数量。
- 检查事务是否遗漏提交或回滚，异常分支是否遗漏关闭连接。
- 对照流量变化，确认是容量不足、慢查询占用，还是代码连接泄漏。

## 处理原则
- 先释放异常连接和修复泄漏，再按数据库容量调整连接池。
- 不能只提高最大连接数，否则可能把压力转移到数据库。

