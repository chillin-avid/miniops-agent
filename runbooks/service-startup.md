# 服务启动失败

## 典型现象
- 进程启动后立即退出，健康检查一直失败，或端口没有监听。
- 常见原因包括配置缺失、端口占用、依赖不可达和权限不足。

## 排查步骤
- 先读取启动阶段第一条异常，而不是只看最后的退出信息。
- 校验环境变量和配置文件，检查监听端口与文件权限。
- 确认数据库、缓存和消息队列等必要依赖是否可达。

## 处理原则
- 使用上一版本配置做对照，但不要把敏感配置直接打印到日志。

