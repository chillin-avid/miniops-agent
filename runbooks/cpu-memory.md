# CPU 过高与内存异常

## CPU 排查
- 对照发布和流量时间，查看是单实例还是全部实例升高。
- 使用进程和线程采样定位热点，检查死循环、频繁序列化和重试风暴。
- CPU 高并不等于实例数量不足，应先确认计算热点来源。

## 内存排查
- 区分瞬时峰值、稳定高水位和持续增长，持续增长更像泄漏。
- 查看 OOM、垃圾回收时间、对象数量和缓存大小。
- 保留堆转储或采样证据后再重启，否则会丢失定位线索。

## 处理原则
- 紧急扩容可以止损，但后续仍要用采样结果修复根因。

