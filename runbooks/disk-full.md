# 磁盘空间不足

## 典型现象
- 出现 `No space left on device`，日志无法写入，数据库或容器可能异常退出。
- inode 用尽时即使显示仍有容量也无法创建新文件。

## 排查步骤
- 同时检查磁盘容量和 inode，定位增长最快的目录。
- 检查日志轮转、临时文件、容器镜像和数据库文件。
- 删除前确认文件归属和保留要求，不要直接执行大范围清理命令。

## 处理原则
- 先扩容或清理明确可删除的临时数据，再修复轮转和保留策略。

