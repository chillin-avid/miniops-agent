# Redis 连接超时

## 典型现象
- 应用出现 `RedisTimeoutError`、读取缓存超时或延迟突然升高。
- 多个服务同时异常时，优先检查 Redis 实例和网络；只有单个服务异常时，先检查该服务的连接配置。

## 排查步骤
- 确认 Redis 地址、端口、密码和 TLS 配置是否近期变更。
- 查看连接池使用量、慢查询和实例 CPU，区分连接耗尽与服务端变慢。
- 检查客户端超时是否过低，不要在未定位原因前反复扩大连接池。

## 处理原则
- 网络或实例恢复后再观察错误率，连接池耗尽时应先查连接泄漏。
- 临时增加超时只能缓解抖动，不能替代根因排查。

