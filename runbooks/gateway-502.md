# 网关 502 与 504

## 典型现象
- 网关返回 502 通常表示上游连接失败或响应格式异常。
- 504 通常表示上游在网关超时前没有返回结果。

## 排查步骤
- 按请求时间和 trace id 对照网关与上游服务日志。
- 检查上游实例健康、DNS、端口、连接拒绝和响应耗时。
- 只影响单一路由时检查路由配置；全站异常时检查网关和公共依赖。

## 处理原则
- 先恢复不可用实例或错误路由，再分析容量与慢调用。
- 不要只把网关超时调大，这会延长用户等待并掩盖上游故障。

